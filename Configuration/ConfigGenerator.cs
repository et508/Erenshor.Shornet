using BepInEx;

namespace ShorNet
{
    public static class ConfigGenerator
    {
        public static void GenerateConfig(BaseUnityPlugin baseUnityPlugin)
        {
            // Connection settings are managed via ShorNetSetup / connection.wtf.
            // User-facing BepInEx config entries will be added here as needed.
        }
    }
}